services:
  web:
    build: .
    container_name: express-js-chatbot
    ports:
      - "8080:8080"   # acessa via http://SEU_IP:8080
    environment:
      - NODE_ENV=development
      - PORT=8080
    restart: unless-stopped
